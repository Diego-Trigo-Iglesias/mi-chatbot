# ChatBot FullStack

Este proyecto contiene:

- 📦 **Frontend (Vue + Vite):** `/frontend`
- ⚙️ **Backend (Spring Boot):** `/backend`

## Cómo ejecutar

### Backend (Spring Boot)
```bash
cd backend
./mvnw spring-boot:run

